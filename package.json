{
  "name": "aispaces",
  "version": "1.0.0",
  "description": "Manages the client and API for production deployment.",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix client && npm install --prefix api-backend",
    "build": "npm run build --prefix client && npm run build --prefix api-backend",
    "start": "npm run build && npm run serve --prefix api-backend",
    "dev": "npm run dev --prefix client & npm run dev --prefix api-backend"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "client",
    "api-backend"
  ]
}